import { TechniqueSetFilterPipe } from './technique-set-filter.pipe';

describe('TechniqueSetFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TechniqueSetFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
