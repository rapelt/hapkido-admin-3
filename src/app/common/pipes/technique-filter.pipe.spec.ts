import { TechniqueFilterPipe } from './technique-filter.pipe';

describe('TechniqueFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TechniqueFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
