import { SortTechniquesPipe } from './sort-techniques.pipe';

describe('SortTechniquesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortTechniquesPipe();
    expect(pipe).toBeTruthy();
  });
});
