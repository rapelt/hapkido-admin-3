import { SortTechniquesSetsPipe } from './sort-techniques-sets.pipe';

describe('SortTechniquesSetsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortTechniquesSetsPipe();
    expect(pipe).toBeTruthy();
  });
});
